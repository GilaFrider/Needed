
--    [area]                     VARCHAR (60)  NOT NULL,
--    [salary]                   INT           NOT NULL,
--    [SeveralYearsOfExperience] INT           NOT NULL,
--    [DatePublished]            DATE          NOT NULL,



--CREATE TABLE [dbo].[Jobs] (
--    [Code]                     INT           IDENTITY (1, 1) NOT NULL,
--    [businessName]             VARCHAR (100) NOT NULL,
--    [fieldOfWorkCode]              VARCHAR (20)  NOT NULL,
--    PRIMARY KEY CLUSTERED ([Code] ASC),
--    ADD CONSTRAINT FK_PersonOrder
--    FOREIGN KEY (fieldOfWork) REFERENCES Persons(PersonID);
    
--);

create table tchoomO(
    code 
    tchoom
);
create table kriterion(
    code 
    kriterion
);
create table tchoomOlekriterion(
    code 
    tchoomcode
    kriterioncode
);

